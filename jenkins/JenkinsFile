pipeline {
    agent any 
    stages {
        stage("build") {
            steps {
                sh "echo hi there"
                sh "ng build --configuration=production --base-href=/vb/"
            }
        }
    }
}
