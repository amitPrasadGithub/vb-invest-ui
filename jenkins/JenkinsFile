pipeline {
    agent any 
    stages {
        stage("build") {
            steps {
              bash "echo hello"  
              bash "ng build --configuration=production --base-href=/vb/"
            }
        }
    }
}
