pipeline {
    agent any 
    environment {
        def branch = env.GIT_BRANCH
    }
    stages {
        stage("build") {
            steps {
                sh "echo ${branch}"
                sh "echo ## Bingo ##"
            }
        }
    }
}
