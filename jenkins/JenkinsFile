pipeline {
    agent any 
    stages {
        stage("build") {
            steps {
                echo "Hi there" >> /tmp/jenkinsFile.txt
                sh "ng build --configuration=production --base-href=/vb/"
            }
        }
    }
}
