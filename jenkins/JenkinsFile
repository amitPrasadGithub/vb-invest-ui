pipeline {
    agent any 
    stages {
        stage("build") {
            steps {
                sh "echo Hi there > /tmp/jenkins.txt"
                sh "ng build --configuration=production --base-href=/vb/"
            }
        }
    }
}
